# from django import template

# register = template.Library()

# @register.filter
# def mul(value, arg):
#     print(f"Value: {value}, Arg: {arg}")
#     try:
#         return float(value) * float(arg)
#     except (ValueError, TypeError):
#         return 0


# # Es para utilizar una multiplicación en un template de Django.
# eSTA COMENTADO PORQUE ALGO SE ROMPIO